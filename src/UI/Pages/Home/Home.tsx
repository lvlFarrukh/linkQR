import React, { useState } from "react";
import QR from "../../Components/QR";

type Props = {};

const Home = (props: Props) => {

  return (
    <div>
      <QR />
    </div>
  );
};

export default Home;
